# 自动化爬虫服务提示词

## 背景说明

本项目已有三个功能脚本目录：
- `crawler/`：基于 Playwright + CDP 的自动化爬虫，爬取山西电力交易平台现货市场信息披露数据，输出 CSV 或者 Excel 文件
- `data-verify/`：对爬取的 CSV/Excel 数据进行质量校验（日期覆盖、字段完整性、数值范围、日期连续性等）
- `post-process/`：对校验通过的数据进行转换，生成规范宽表 CSV，输出到 `output/` 目录

---

## 提示词

---

你是一名精通 Python 微服务架构的后端工程师，请基于以下已有脚本和需求，设计并实现一套**自动化爬虫服务**。

### 一、已有脚本说明

**1. 爬虫脚本（`crawler/`）**

- 入口：`crawler/main.py`
- 功能：通过 Playwright CDP 连接已登录的 Chrome，自动爬取山西电力交易平台 19 类现货市场数据，按日迭代，支持增量更新，输出 CSV 或者 Excel 文件到 `crawler/data/` 目录
- 关键参数：`--task`（任务名，逗号分隔多个）、`--start`（起始日期）、`--end`（结束日期）、`--schedule`（定时模式，按 `interval_hours` 间隔循环执行）、`--validate`（仅执行数据质量校验）、`--loss-file`（从 `data-verify/loss.txt` 批量补充下载缺失数据）、`--list-tasks`（列出所有可用任务）
- 配置文件：`crawler/config.yaml`（任务开关、日期范围、反爬间隔、CDP 地址、调度间隔等）

**2. 数据校验脚本（`data-verify/`）**

- 入口：`data-verify/analyze_excel.py`
- 功能：通过 `data-verify/config.yaml` 配置多个校验对象（validator），对 `crawler/data/` 目录下的 CSV/Excel 文件执行以下校验：
  - 文件名日期与表格内容日期一致性校验
  - 文件名通道名与表格内容通道名一致性校验（仅 `prefix_date_channel` 格式）
  - 指定日期范围内文件完整性校验（含通道缺失/重复检测）
  - 对内容有误（日期/通道不一致、文件无法解析）的文件自动删除，再统计真正缺失的文件
- 输入：`crawler/data/` 目录下的 CSV 和 Excel（.xlsx）文件
- 输出：`data-verify/loss.txt`（缺失文件列表，供 `--loss-file` 参数使用）+ `data-verify/validation_errors.txt`（错误详细报告）

**3. 数据转换脚本（`post-process/`）**

- 入口：`post-process/batch_process_data.py`
- 功能：将 CSV/Excel 转换为规范宽表，统一时间格式（`YYYY-MM-DD HH:MM:SS.000`），处理枚举映射（开停状态、是否越限等），单文件超 5MB 自动拆分
- 输入：`post-process/data/` 目录下按任务名分子目录的 CSV 或 Excel 文件（`*.csv` 或 `*.xlsx`）
- 输出：`post-process/output/` 目录下按数据类别分子目录的宽表 CSV

---

### 二、目标架构

将完整自动化流程拆分为**两个微服务**，分别部署在两台服务器上：

```
┌─────────────────────────────────────────────────────────────────┐
│  服务器 A：采集服务（crawler-service）                            │
│                                                                  │
│  自动爬取 → 自动校验 → [校验失败则重新爬取] → 自动打包上传         │
└─────────────────────────────────────────────────────────────────┘
                              │ 上传完成后通知（HTTP 回调 / 消息队列）
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  服务器 B：处理服务（processor-service）                          │
│                                                                  │
│  自动数据转换 → 自动打包上传 → 自动通知三方平台                     │
└─────────────────────────────────────────────────────────────────┘
```

---

### 三、详细需求

#### 3.1 服务器 A：采集服务（`crawler-service/`）

**完整流程：自动爬取 → 自动校验 → [失败重爬] → 自动打包上传**

**Step 1：自动爬取**

支持两种触发模式，通过启动参数区分：

**模式一：定时日增模式（`--mode scheduled`）**
- `crawler-service/main.py` 启动后使用 APScheduler 的 `CronTrigger` 进入阻塞调度循环，触发时间通过 `crawler-service/config.yaml` 的 `schedule.cron` 以标准 cron 表达式配置（如 `"30 8 * * *"`），支持任意复杂调度规则，权责清晰，与 `crawler/config.yaml` 无耦合
- 每次触发时根据 `schedule.date_offset_days`（默认 `-1`，即昨天）计算目标日期，以**单次模式**（带 `--start`/`--end`，不带 `--schedule`）调用 `crawler/main.py`，爬虫执行完毕后进程退出，再衔接后续校验→打包→通知流程
- 适用于日常自动化运行

**模式二：手动批量模式（`--mode batch`）**
- 手动触发，通过命令行参数指定日期范围和任务范围
- 参数：`--start YYYY-MM-DD`、`--end YYYY-MM-DD`、`--task 任务名1,任务名2`（不指定则爬取全部任务）
- 适用于历史数据补录、指定任务重跑等场景

两种模式均支持增量更新（已有数据自动跳过），爬取完成后将本次爬取的起止日期传递给校验步骤。爬虫进程非零退出时，记录 ERROR 日志并**终止当前流程**，不进入校验步骤。

**Step 2：自动校验**
- 调用 `data-verify/analyze_excel.py` 对本次爬取的数据进行校验；`analyze_excel.py` 通过 `--start` 和 `--end` 参数接收日期范围，`verify_runner.py` 在调用时将本次爬取的起止日期作为这两个参数传入
- **`analyze_excel.py` 输出路径**：`loss.txt` 和 `validation_errors.txt` 的输出目录由 `data-verify/config.yaml` 的 `global.output_directory` 字段控制；该字段默认为 `null`，表示输出到脚本自身所在目录，即 `data-verify/loss.txt` 和 `data-verify/validation_errors.txt`；部署时保持该配置为 `null` 即可，无需额外设置；`verify_runner.py` 读取的 `config.verify.loss_file`（`../data-verify/loss.txt`）与此路径一致，两者保持对应
- **校验日期范围与爬取范围完全相同**：
  - 定时日增模式：校验日期范围为昨天（与爬取目标日期相同），即 `--start` 和 `--end` 均传入昨天的日期
  - 手动批量模式：校验日期范围与爬取时指定的 `--start` / `--end` 完全相同
  - 校验日期范围通过命令行参数传入 `data-verify/analyze_excel.py`，与本次爬取的 `--start`/`--end` 参数保持一致
- 校验维度：文件名日期与表格内容日期一致性、文件名通道与表格内容通道一致性、文件完整性（含通道缺失/重复检测）；内容有误的文件自动删除后再统计缺失
- **批量处理**：对所有待校验文件一次性完成校验，生成统一的校验报告，避免逐文件多次触发重爬
- 校验结果判断方法：读取 `data-verify/loss.txt`，跳过以 `#` 开头的注释行和空行，若**不存在任何有效数据行**则判定为 `PASS`，否则判定为 `FAIL`
  - `PASS`：`loss.txt` 中无有效数据行（全部为注释/空行），进入打包上传步骤
  - `FAIL`：`loss.txt` 中存在有效数据行（格式：`名称,日期[,通道名称]`），进入重爬步骤

**Step 3：校验失败自动重爬（批量）**
- 从 `data-verify/loss.txt`（格式：`名称,日期[,通道名称]`）中提取所有失败的任务名称和日期，**批量汇总**后一次性触发重爬
- 重爬时调用 `crawler/main.py`，通过 `--loss-file` 参数传入 `loss.txt` 的绝对路径（由 `verify.loss_file` 配置项在运行时解析为绝对路径后传入），实现精准补充下载
- 重爬完成后再次执行校验（Step 2）
- **最大重爬轮次：3 轮**（即最多执行 3 次重爬，初始校验不计入轮次，完整流程为：初始校验 → 第 1 轮重爬 → 校验 → 第 2 轮重爬 → 校验 → 第 3 轮重爬 → 校验，共 4 次校验）；每轮重爬完成后立即执行本轮校验：校验通过则进入 Step 4 打包上传；校验不通过则等待 **30 秒**后开启下一轮重爬；超出最大轮次后，记录告警日志（包含仍缺失的文件列表），**继续执行 Step 4 打包上传**，将当前已有数据打包上传，不因部分数据缺失而阻塞整个流程
- 重爬策略：仅针对失败的任务+日期组合，不重复爬取已通过的数据

**Step 4：自动打包上传**
- **打包前整理**：分两步完成文件归类，最终结构参考 `post-process/data/`：

  **第一步：展平现有子目录**
  `crawler/data/` 下可能已存在子目录（历史归类结果），需先将所有子目录中的文件移动到 `crawler/data/` 根目录下，再删除已清空的子目录。移动时若根目录已存在同名文件，以子目录中的文件覆盖；若多个子目录中存在同名文件，按子目录名字典序从小到大依次覆盖，最终保留字典序最大的子目录中的文件。

  **第二步：按任务名重新归类**
  将根目录下所有文件按任务名归类到子目录：
  ```
  crawler/data/
  ├── 日前联络线计划/
  │   ├── 日前联络线计划_2025-01-01_京津唐.xlsx
  │   ├── 日前联络线计划_2025-01-01_华北负荷.xlsx
  │   └── ...
  ├── 机组实际发电曲线/
  │   ├── 机组实际发电曲线_2025-01-01.xlsx
  │   └── ...
  └── ...（其他任务名子目录）
  ```
  归类规则：从文件名中提取任务名（第一个 `_` 之前的部分），在 `crawler/data/` 下创建同名子目录，将文件移入对应子目录。整理操作在原目录内完成，不复制文件。
- 将整理后的 `crawler/data/` 目录整体打包为一个 `.tar.gz`，命名为本次爬取的起始日期（即 `start` 参数对应的日期），格式：`YYYY-MM-DD.tar.gz`（如 `2025-01-13.tar.gz`）
- 上传到指定存储（对象存储 / SFTP / 共享目录，通过配置文件指定）
- 上传完成后，通知服务器 B 开始处理（HTTP 回调 / 消息队列 / 写入共享数据库状态表）；通知失败时自动重试，重试次数和间隔通过 `notify.retry_times`（默认 3）和 `notify.retry_interval_seconds`（默认 30）控制，超出后记录告警，不阻塞后续 cleanup 步骤；若服务器 B 返回 `409 Conflict`（幂等响应，表示该任务已在处理中），视为通知成功，不触发重试
- 通知内容：本次上传的 tar 包对象名（不含 prefix，如 `2025-01-13.tar.gz`）、MinIO 下载链接（预签名 URL 或直接拼接地址）、文件 MD5、日期范围、数据类别列表
- **数据类别列表（`categories`）获取规则**：`pipeline.run()` 接收 `tasks` 参数，该参数即为本次爬取的任务名列表；定时日增模式下 `tasks=None`，此时 `pipeline.run()` 内部通过 `config['crawler']['config_path']` 定位并读取 `crawler/config.yaml`，提取所有 `enabled: true` 的任务名作为 categories；手动批量模式下 `tasks` 为 `--task` 参数指定的列表，若未指定则同定时模式逻辑；categories 在 `pipeline.run()` 入口处确定，贯穿整个流程直至传入 `notifier`

  `crawler/config.yaml` 中任务以任务名为 key 的字典形式组织在顶层 `tasks` 字段下，每个任务包含 `enabled` 开关字段，示例结构如下：
  ```yaml
  tasks:
    日前市场出清概况:
      enabled: true
      # ...其他字段
    日前备用总量:
      enabled: true
      # ...其他字段
    实时市场出清概况:
      enabled: false
      # ...其他字段
  ```
  提取逻辑：`[name for name, cfg in crawler_cfg["tasks"].items() if cfg.get("enabled")]`
- 记录上传清单（tar 包对象名、文件大小、MD5、上传时间），以 JSON 格式写入日志目录，文件名为 `upload_manifest_YYYYMMDD_HHmmss.json`（时间戳取上传开始执行时的本地时间，如 `upload_manifest_20250113_083045.json`），格式示例：`{"object_name": "2025-01-13.tar.gz", "size_bytes": 1048576, "md5": "abc123...", "uploaded_at": "2025-01-13T08:30:45"}`；同时通过日志系统输出摘要
- 上传完成后，若配置项 `crawler.cleanup_after_upload` 为 `true`，则清除 `crawler/data/` 目录下的所有数据，释放磁盘空间；为 `false` 时保留数据，便于排查问题；清理操作仅依赖上传是否成功，上传失败时不清理，通知失败不影响清理操作的执行

**服务配置（`crawler-service/config.yaml`）：**
```yaml
schedule:
  cron: "30 8 * * *"          # 定时日增模式触发时间，标准 5 字段 cron 表达式（分 时 日 月 周）
  timezone: "Asia/Shanghai"
  date_offset_days: -1        # 目标日期相对触发日期的偏移量（-1=昨天，-2=前天，以此类推）

crawler:
  script_path: "../crawler/main.py"
  config_path: "../crawler/config.yaml"
  data_dir: "../crawler/data"
  cleanup_after_upload: false        # 上传完成后是否清除 crawler/data/ 目录（true=清除，false=保留）
  # 手动批量模式示例（命令行启动时传入）：
  # python main.py --mode batch --start 2025-01-01 --end 2025-03-31 --task 日前备用总量,断面约束

verify:
  script_path: "../data-verify/analyze_excel.py"
  config_path: "../data-verify/config.yaml"
  loss_file: "../data-verify/loss.txt"   # 校验生成的缺失文件列表，供重爬时 --loss-file 参数使用
  max_retry_rounds: 3         # 最大重爬轮次（最多执行 3 次重爬，初始校验不计入）
  retry_interval_seconds: 30  # 校验不通过后等待时间（秒），再开启下一轮重爬

upload:
  type: "minio"
  endpoint: "http://minio-server:9000"  # MinIO 服务地址
  access_key: "${CRAWLER_MINIO_ACCESS_KEY}"   # MinIO 账号（通过环境变量注入）
  secret_key: "${CRAWLER_MINIO_SECRET_KEY}"   # MinIO 密码（通过环境变量注入）
  bucket: "sxpx"                    # 存放原始爬取数据的 bucket
  prefix: "raw/"
  secure: false                         # 是否使用 HTTPS

notify:
  enabled: true               # 是否启用通知服务器 B（false 时跳过通知，流程在上传完成后结束）
  type: "http"                # http / rabbitmq / redis
  url: "http://processor-service:8080/api/trigger"
  secret: "${CRAWLER_NOTIFY_SECRET}"    # 通过环境变量注入
  retry_times: 3              # 通知失败最大重试次数
  retry_interval_seconds: 30  # 每次重试间隔（秒）

log_dir: "./logs"             # 日志及上传清单（upload_manifest_*.json）输出目录
```

---

#### 3.2 服务器 B：处理服务（`processor-service/`）

**完整流程：自动数据转换 → 自动打包上传 → 自动通知三方平台**

**Step 1：接收触发信号**
- 提供 HTTP 接口（`POST /api/trigger`）接收服务器 A 的通知
- 触发信号中包含：tar 包对象名、MinIO 下载链接、文件 MD5、日期范围、数据类别列表
- **请求体字段定义（Pydantic 模型）**：
  ```python
  from pydantic import BaseModel
  from typing import Optional

  class DateRange(BaseModel):
      start: str   # 格式 YYYY-MM-DD
      end: str     # 格式 YYYY-MM-DD

  class TriggerRequest(BaseModel):
      object_name: str          # tar 包文件名，不含 prefix，如 "2025-01-13.tar.gz"，作为幂等键
      download_url: str         # MinIO 直接访问地址，仅作参考备用
      md5: str                  # tar 包 MD5，用于下载后完整性校验
      date_range: DateRange     # 爬取日期范围
      categories: list[str]     # 数据类别列表（任务名列表），如 ["机组实际发电曲线", "日前联络线计划"]
      timestamp: str            # 服务器 A 发送通知时的 ISO 8601 时间戳
  ```
- **幂等处理**：以 `object_name`（tar 包对象名）作为幂等键，若同一 `object_name` 的任务已在处理中或已处理完成，则返回 `409 Conflict` 并记录日志，不重复触发处理流程，防止服务器 A 因网络超时重发通知导致重复处理；幂等状态需**持久化到本地文件**，服务启动时自动加载，防止重启后状态丢失；文件路径对应关系：宿主机 `./data/processor-service/processed_jobs.json` → 容器内 `/app/processor-service/processed_jobs.json`，配置文件中以 `jobs_file: "./processed_jobs.json"` 引用（容器内相对于工作目录 `/app/processor-service`）；状态记录格式如下：
  ```json
  {
    "2025-01-13.tar.gz": {"status": "done", "started_at": "2025-01-14T08:31:00", "finished_at": "2025-01-14T08:45:00"},
    "2025-01-14.tar.gz": {"status": "processing", "started_at": "2025-01-15T08:31:00", "finished_at": null}
  }
  ```
  `status` 取值：`processing`（处理中）、`done`（已完成）、`failed`（处理失败，可重新触发）；`failed` 状态不触发 `409`，允许重新处理
- **幂等键冲突说明**：`object_name` 由爬取起始日期生成（如 `2025-01-13.tar.gz`），同一天内若需重新触发处理（如手动批量模式多次执行），需先将 `processed_jobs.json` 中对应记录的 `status` 改为 `failed`（或删除该记录），再重新触发；`done` 和 `processing` 状态均会返回 `409`，不会重复处理
- **异步处理**：接口收到请求后，将 `object_name` 的幂等状态置为 `processing` 并**立即返回 `202 Accepted`**，实际处理流程（下载→解压→转换→上传→通知）在后台线程中异步执行；若同步等待处理完成再返回，服务器 A 的 HTTP 请求（`timeout=30`）将超时，触发重试，导致重复处理
- **启动时超时重置**：服务启动时扫描 `processed_jobs.json`，将所有 `status` 为 `processing` 且 `started_at` 距当前时间超过 `server.processing_timeout`（默认 600 秒）的记录重置为 `failed`，防止服务异常重启后任务永久卡死在 `processing` 状态无法重新触发；重置时记录 WARNING 日志，说明被重置的 `object_name` 和原始 `started_at`
- **鉴权（预留）**：`config.yaml` 中已预留 `server.secret` 字段，`notifier.py` 发送请求时携带 `X-Secret` 请求头，`api.py` 当前**不做鉴权校验**，仅透传忽略该头；待后续有需要时，在 `api.py` 中补充对 `X-Secret` 与 `server.secret` 的比对逻辑即可，无需改动配置和调用方

**Step 2：下载解压 + 自动数据转换**
- 根据触发信号中的 `object_name`（不含 prefix 的文件名，如 `2025-01-13.tar.gz`），结合自身 `storage` 配置（endpoint/bucket/prefix）自行构造完整下载路径（即 `{prefix}{object_name}`），将 tar 包下载到 `storage.local_cache_dir` 指定的本地缓存目录；通知中的 `download_url` 仅作参考备用
- 校验下载文件的 MD5，与通知中的 MD5 一致后再继续，不一致则记录告警并终止
- 将 tar 包解压到 `post-process/data/` 目录；**解压前先清空 `post-process/data/` 和 `post-process/output/` 两个目录**（若存在旧数据），确保每次处理的数据范围与本次触发一致，避免新旧数据混合（`output/` 若不提前清空，旧转换结果会与本次新结果混合，导致打包时包含历史数据）；由于服务器 A 打包时以 `arcname=os.path.basename(source_dir)`（即 `"data"`）作为顶层目录名，tar 包内结构为 `data/任务名/文件`，解压时需**剥离顶层的 `data/` 目录**，将其内容直接写入 `post-process/data/`，示例如下：
  ```python
  import tarfile, os

  def extract_strip_top(tar_path: str, dest_dir: str):
      """解压 tar 包并剥离顶层目录，将内容直接写入 dest_dir"""
      with tarfile.open(tar_path, "r:gz") as tar:
          for member in tar.getmembers():
              # 去掉第一个路径分量（顶层目录名 "data"）
              parts = member.name.split("/", 1)
              if len(parts) < 2 or not parts[1]:
                  continue  # 跳过顶层目录本身
              member.name = parts[1]
              tar.extract(member, path=dest_dir)
  ```
  解压后的目录结构按任务名分子目录，例如：
  ```
  post-process/data/
  ├── 二次调频出清结果/
  │   ├── 二次调频出清结果_2025-01-01.xlsx
  │   └── ...
  ├── 机组实际发电曲线/
  │   ├── 机组实际发电曲线_2025-01-01.xlsx
  │   └── ...
  └── ...（其他任务名子目录）
  ```
- 调用 `post-process/batch_process_data.py` 进行宽表转换
- 转换规则：
  - 统一时间格式：`YYYY-MM-DD HH:MM:SS.000`
  - 枚举映射：开停状态（停机→0，开机→1）、是否越限（否→0，是→1）
  - 单文件超 5MB 自动拆分为 `*_part1.csv`、`*_part2.csv`
- 输出到 `post-process/output/` 目录，按数据类别建子目录
- **异常处理**：下载失败、MD5 校验不一致、解压失败、转换脚本非零退出等任意不可恢复错误发生时，将该 `object_name` 的幂等状态更新为 `failed` 并记录详细错误日志；`failed` 状态允许服务器 A 重新触发处理，不会返回 `409`

**Step 3：自动打包上传**
- 将 `post-process/output/` 目录整体打包为一个 `.tar.gz`，命名使用触发信号中 `date_range.start` 的日期，与服务器 A 的命名基准保持一致（均使用爬取起始日期），格式：`YYYY-MM-DD.tar.gz`（如 `2025-01-13.tar.gz`），确保与服务器 A 的原始包可通过文件名追溯对应关系
- 上传到指定存储（对象存储 / SFTP / 共享目录，通过配置文件指定）
- 记录上传清单（tar 包对象名、文件大小、MD5、上传时间），以 JSON 格式写入日志目录，文件名为 `upload_manifest_YYYYMMDD_HHmmss.json`（时间戳取上传开始执行时的本地时间，如 `upload_manifest_20250113_083045.json`），格式示例：`{"object_name": "2025-01-13.tar.gz", "size_bytes": 2097152, "md5": "def456...", "uploaded_at": "2025-01-13T08:45:00"}`；同时通过日志系统输出摘要
- 上传完成后，若配置项 `processor.cleanup_after_upload` 为 `true`，则清除 `post-process/data/` 和 `post-process/output/` 目录下的所有数据，释放磁盘空间；为 `false` 时保留数据，便于排查问题；清理操作仅依赖上传是否成功，上传失败时不清理，通知失败不影响清理操作的执行

**Step 4：自动通知三方平台**
- 打包上传完成后，调用三方平台 API 通知数据就绪
- 通知内容：数据类别、日期范围、文件路径/下载地址、文件 MD5
- 支持多个三方平台（通过配置文件列表配置）
- 通知失败时自动重试，重试次数和间隔均通过配置文件控制（默认最多 5 次，每次间隔 30 秒），超出后记录告警；某个平台通知失败不影响其他平台的通知，所有平台均独立尝试，全部完成后流程结束

**服务配置（`processor-service/config.yaml`）：**
```yaml
server:
  host: "0.0.0.0"
  port: 8080
  secret: "${CRAWLER_PROCESSOR_SERVER_SECRET}"           # 通过环境变量注入，与 .env 中 CRAWLER_PROCESSOR_SERVER_SECRET 对应
  jobs_file: "./processed_jobs.json"    # 幂等状态持久化文件，服务启动时自动加载
  processing_timeout: 600          # processing 状态超时阈值（秒），启动时超时记录重置为 failed

storage:
  type: "minio"
  endpoint: "http://minio-server:9000"  # 与服务器 A 保持一致
  access_key: "${CRAWLER_MINIO_ACCESS_KEY}"   # 通过环境变量注入
  secret_key: "${CRAWLER_MINIO_SECRET_KEY}"   # 通过环境变量注入
  bucket: "sxpx"                    # 从该 bucket 下载服务器 A 上传的原始数据
  prefix: "raw/"
  secure: false
  local_cache_dir: "./cache"            # 下载后的本地缓存目录

processor:
  script_path: "../post-process/batch_process_data.py"
  data_dir: "../post-process/data"           # tar 包解压目标目录
  output_dir: "../post-process/output"
  cleanup_after_upload: true                 # 上传完成后是否清除 data/ 和 output/ 目录（true=清除，false=保留）

upload:
  type: "minio"
  endpoint: "http://minio-server:9000"  # MinIO 服务地址
  access_key: "${CRAWLER_MINIO_ACCESS_KEY}"   # 通过环境变量注入
  secret_key: "${CRAWLER_MINIO_SECRET_KEY}"   # 通过环境变量注入
  bucket: "sxpx"                 # 存放宽表转换结果的 bucket
  prefix: "output/"
  secure: false

notify:
  platforms:
    - name: "platform-a"
      enabled: true           # 单独控制每个平台的通知开关
      url: "https://api.platform-a.com/data/notify"
      token: "platform-a-token"
      retry_times: 5          # 最大重试次数
      retry_interval_seconds: 30  # 每次重试间隔（秒）
    - name: "platform-b"
      enabled: false          # 关闭时跳过该平台，不影响其他平台
      url: "https://api.platform-b.com/webhook"
      token: "platform-b-token"
      retry_times: 5
      retry_interval_seconds: 30

log_dir: "./logs"             # 日志及上传清单（upload_manifest_*.json）输出目录
```

---

### 四、项目目录结构

```
auto-crawler/
├── crawler/                        # 已有：爬虫脚本（不修改）
├── data-verify/                    # 已有：数据校验脚本（不修改）
├── post-process/                   # 已有：数据转换脚本（不修改）
│
├── crawler-service/                # 新建：采集服务（部署在服务器 A）
│   ├── base/                       # 采集服务基础镜像
│   │   ├── Dockerfile              # 基础镜像：系统依赖 + Chrome/Playwright + 公共 Python 包
│   │   └── requirements-base.txt   # 基础镜像 Python 依赖
│   ├── main.py                     # 服务主入口（定时调度 + 流程编排）
│   ├── pipeline.py                 # 流程编排：爬取→校验→重爬→打包上传
│   ├── crawler_runner.py           # 调用 crawler/main.py 的封装
│   ├── verify_runner.py            # 调用 data-verify/analyze_excel.py 的封装（含 loss.txt 解析）
│   ├── uploader.py                 # 打包上传（S3/SFTP/本地）
│   ├── notifier.py                 # 通知服务器 B
│   ├── config_loader.py            # 配置加载工具（含 ${ENV_VAR} 环境变量替换）
│   ├── logger.py                   # 日志初始化工具（setup_logger）
│   ├── config.yaml                 # 服务配置模板
│   ├── requirements.txt            # 业务层追加依赖
│   └── Dockerfile                  # 服务镜像：FROM crawler-service-base
│
├── processor-service/              # 新建：处理服务（部署在服务器 B）
│   ├── base/                       # 处理服务基础镜像
│   │   ├── Dockerfile              # 基础镜像：系统依赖 + 公共 Python 包
│   │   └── requirements-base.txt   # 基础镜像 Python 依赖
│   ├── main.py                     # 服务主入口（HTTP 服务 + 流程编排）
│   ├── api.py                      # HTTP 接口（接收触发信号）
│   ├── processor_runner.py         # 调用 post-process/batch_process_data.py 的封装
│   ├── uploader.py                 # 打包上传（S3/SFTP/本地）
│   ├── platform_notifier.py        # 通知三方平台
│   ├── config_loader.py            # 配置加载工具（含 ${ENV_VAR} 环境变量替换）
│   ├── logger.py                   # 日志初始化工具（setup_logger）
│   ├── config.yaml                 # 服务配置
│   ├── requirements.txt            # 业务层追加依赖
│   └── Dockerfile                  # 服务镜像：FROM processor-service-base
│
└── docker-compose.yml              # 统一部署入口（两个服务均通过此文件构建和启动）
```

---

### 五、技术要求

- **语言**：Python 3.9+
- **定时调度**：APScheduler（`CronTrigger`）（服务器 A）
- **HTTP 服务**：FastAPI 或 Flask（服务器 B）
- **进程调用**：`subprocess.run()` 调用已有脚本，捕获 stdout/stderr
- **上传**：`minio`（MinIO Python SDK）连接 MinIO，使用 Access Key / Secret Key 认证；打包使用 `tarfile` 标准库
- **通知**：`httpx` 或 `requests`，支持重试和超时
- **日志**：统一日志格式（时间 + 级别 + 模块 + 消息），文件按大小滚动；日志分级规范如下：
  - `DEBUG`：详细的过程信息，仅在调试时开启，包括：subprocess 的完整 stdout/stderr 输出、每个文件的处理结果、HTTP 请求/响应的完整内容（含 headers、body）、MinIO 操作的详细参数（bucket、object_name、size）、配置加载后的最终值（敏感字段脱敏后打印）
  - `INFO`：正常流程的关键节点，包括：服务启动/停止、每个 Step 的开始与完成（含耗时）、爬取/校验/上传/通知的成功结果摘要、定时任务触发时间和目标日期、重爬轮次开始
  - `WARNING`：可恢复的异常或需关注的状态，包括：校验失败（含缺失文件数量）、重爬轮次开始（含当前轮次/最大轮次）、通知重试（含当前次数/最大次数和错误原因）、达到最大重爬轮次仍有缺失（含缺失文件列表）、启动时重置 `processing` 超时任务（含 object_name 和原始 started_at）、环境变量未设置（含变量名）
  - `ERROR`：不可恢复的错误，包括：subprocess 非零退出（含 returncode、stderr 摘要）、MD5 校验不一致（含期望值和实际值）、MinIO 上传失败（含 object_name 和异常信息）、通知达到最大重试次数仍失败（含目标 URL 和最后一次异常）、下载/解压失败（含文件路径和异常信息）
  - 日志初始化统一封装为 `setup_logger(name, log_dir, level)` 函数，支持同时输出到控制台（StreamHandler）和滚动文件（RotatingFileHandler，单文件最大 10MB，保留 5 个备份）；生产环境默认级别 `INFO`，可通过环境变量 `LOG_LEVEL` 覆盖（如 `LOG_LEVEL=DEBUG`）
- **容器化**：每个服务各自维护独立的基础镜像，采用两层构建结构（详见第六节）；统一通过 `docker compose` 构建和部署

---

### 六、容器化部署要求

两个服务各自维护独立的基础镜像，均采用**两层构建结构**：基础镜像负责环境准备，服务镜像只叠加业务代码，依赖变更时只需重建基础镜像，业务迭代时只需重建服务镜像。

```
python:3.11-slim（上游官方镜像）
       │                          │
       ▼                          ▼
crawler-service-base         processor-service-base
（系统依赖 + Chrome/Playwright   （系统依赖 + 公共 Python 包）
 + 公共 Python 包）
       │                          │
       ▼                          ▼
crawler-service              processor-service
（业务代码）                   （业务代码）
```

**采集服务基础镜像（`crawler-service/base/Dockerfile`）**

包含 Chrome 运行所需的系统库、Playwright 及公共 Python 包：

```dockerfile
FROM python:3.11-slim

RUN apt-get update && apt-get install -y \
    wget gnupg ca-certificates \
    fonts-liberation libappindicator3-1 libasound2 libatk-bridge2.0-0 \
    libatk1.0-0 libcups2 libdbus-1-3 libgdk-pixbuf2.0-0 libgtk-3-0 \
    libnspr4 libnss3 libx11-xcb1 libxcomposite1 libxdamage1 libxrandr2 \
    xdg-utils --no-install-recommends && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY requirements-base.txt .
RUN pip install --no-cache-dir -r requirements-base.txt
RUN playwright install chromium
RUN playwright install-deps chromium
```

**采集服务镜像（`crawler-service/Dockerfile`）**

在基础镜像之上只叠加业务代码和追加依赖：

```dockerfile
FROM crawler-service-base:latest

WORKDIR /app/crawler-service
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 只拷贝业务源文件，config.yaml / logs / 其他运行时文件通过 Volume 挂载
COPY main.py pipeline.py crawler_runner.py verify_runner.py uploader.py notifier.py config_loader.py logger.py ./

# 预创建日志目录并设为全局可写，确保宿主机 Volume 挂载前容器内路径已存在；
# 若宿主机目录已通过 init.sh 设置 chmod 777，此处权限会被挂载覆盖，无副作用
RUN mkdir -p /app/crawler-service/logs && chmod 777 /app/crawler-service/logs

CMD ["python", "main.py"]
```

**注意**：Chrome CDP 连接模式（连接宿主机已登录的 Chrome）时，容器需通过 `--network=host` 或端口映射访问宿主机的 `9222` 端口。

**处理服务基础镜像（`processor-service/base/Dockerfile`）**

包含系统依赖和公共 Python 包，无需 Chrome：

```dockerfile
FROM python:3.11-slim

RUN apt-get update && apt-get install -y \
    ca-certificates --no-install-recommends && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY requirements-base.txt .
RUN pip install --no-cache-dir -r requirements-base.txt
```

**处理服务镜像（`processor-service/Dockerfile`）**

在基础镜像之上只叠加业务代码和追加依赖：

```dockerfile
FROM processor-service-base:latest

WORKDIR /app/processor-service
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 只拷贝业务源文件，config.yaml / logs / processed_jobs.json 通过 Volume 挂载
COPY main.py api.py processor_runner.py uploader.py platform_notifier.py config_loader.py logger.py ./

# 预创建日志目录并设为全局可写，确保宿主机 Volume 挂载前容器内路径已存在；
# 若宿主机目录已通过 init.sh 设置 chmod 777，此处权限会被挂载覆盖，无副作用
RUN mkdir -p /app/processor-service/logs && chmod 777 /app/processor-service/logs

CMD ["python", "main.py"]
```

**构建与部署（统一使用 `docker compose`）**

所有镜像的构建和服务的启动均通过根目录的 `docker-compose.yml` 完成，无需手动执行 `docker build`。

```bash
# 首次部署：必须先构建基础镜像，再构建服务镜像
docker compose --profile build-only build   # 构建两个基础镜像
docker compose build                         # 构建两个服务镜像（依赖上一步的基础镜像）

# 启动所有服务（后台运行）
docker compose up -d

# 依赖变更后（requirements-base.txt 有改动）：重建基础镜像后再重建服务镜像
docker compose --profile build-only build crawler-service-base   # 或 processor-service-base
docker compose build crawler-service                              # 或 processor-service

# 仅业务代码变更：只需重建服务镜像并重启
docker compose up -d --build crawler-service
docker compose up -d --build processor-service

# 查看日志
docker compose logs -f crawler-service
docker compose logs -f processor-service

# 停止服务
docker compose down
```

**`docker-compose.yml`（完整示例）**

```yaml
services:

  # ── 采集服务基础镜像（仅用于构建，不运行）──────────────────────────────
  crawler-service-base:
    build:
      context: ./crawler-service/base
      dockerfile: Dockerfile
    image: crawler-service-base:latest
    # 该服务仅作为构建目标，不需要启动
    profiles: ["build-only"]

  # ── 处理服务基础镜像（仅用于构建，不运行）──────────────────────────────
  processor-service-base:
    build:
      context: ./processor-service/base
      dockerfile: Dockerfile
    image: processor-service-base:latest
    profiles: ["build-only"]

  # ── 采集服务（服务器 A）────────────────────────────────────────────────
  crawler-service:
    build:
      context: ./crawler-service
      dockerfile: Dockerfile
    image: crawler-service:latest
    restart: always             # 服务器重启后自动拉起容器
    network_mode: host          # CDP 连接宿主机 Chrome（9222 端口），需 host 网络
    environment:
      - CRAWLER_MINIO_ACCESS_KEY=${CRAWLER_MINIO_ACCESS_KEY}
      - CRAWLER_MINIO_SECRET_KEY=${CRAWLER_MINIO_SECRET_KEY}
      - CRAWLER_NOTIFY_SECRET=${CRAWLER_NOTIFY_SECRET}
    volumes:
      # 已有脚本目录（读写挂载，爬虫运行时需要写入 crawler/data/；data-verify 需要写入 loss.txt 等输出文件）
      - ./crawler:/app/crawler:rw
      - ./data-verify:/app/data-verify:rw
      # 配置文件（只读挂载，修改配置后重启服务生效）
      - ./crawler-service/config.yaml:/app/crawler-service/config.yaml:ro
      # 数据与日志（读写挂载，持久化到宿主机）
      - ./data/crawler:/app/crawler/data:rw
      - ./data/data-verify/loss.txt:/app/data-verify/loss.txt:rw
      - ./data/data-verify/validation_errors.txt:/app/data-verify/validation_errors.txt:rw
      - ./logs/crawler-service:/app/crawler-service/logs:rw
    depends_on: []              # 采集服务无外部依赖，独立启动

  # ── 处理服务（服务器 B）────────────────────────────────────────────────
  processor-service:
    build:
      context: ./processor-service
      dockerfile: Dockerfile
    image: processor-service:latest
    restart: always             # 服务器重启后自动拉起容器
    ports:
      - "8080:8080"             # 暴露 HTTP 触发接口
    environment:
      - CRAWLER_MINIO_ACCESS_KEY=${CRAWLER_MINIO_ACCESS_KEY}
      - CRAWLER_MINIO_SECRET_KEY=${CRAWLER_MINIO_SECRET_KEY}
      - CRAWLER_PROCESSOR_SERVER_SECRET=${CRAWLER_PROCESSOR_SERVER_SECRET}        # processor-service HTTP 接口鉴权密钥
    volumes:
      # 已有脚本目录（读写挂载，processor 运行时需要向 post-process/data/ 和 post-process/output/ 写入数据）
      - ./post-process:/app/post-process:rw
      # 配置文件（只读挂载）
      - ./processor-service/config.yaml:/app/processor-service/config.yaml:ro
      # 数据与日志（读写挂载，持久化到宿主机）
      - ./data/post-process/data:/app/post-process/data:rw
      - ./data/post-process/output:/app/post-process/output:rw
      - ./data/processor-service/cache:/app/processor-service/cache:rw
      - ./data/processor-service/processed_jobs.json:/app/processor-service/processed_jobs.json:rw  # 需提前写入 {} 创建（不可用 touch，空文件非合法 JSON），否则 Docker 会将其创建为目录
      - ./logs/processor-service:/app/processor-service/logs:rw
```

`.env` 文件（与 `docker-compose.yml` 同级，存放敏感配置，不提交到版本库）：

```dotenv
CRAWLER_MINIO_ACCESS_KEY=your-access-key
CRAWLER_MINIO_SECRET_KEY=your-secret-key
CRAWLER_NOTIFY_SECRET=your-secret
CRAWLER_PROCESSOR_SERVER_SECRET=your-server-secret
```

> **注意事项：**
> - 两个基础镜像服务（`crawler-service-base`、`processor-service-base`）使用 `profiles: ["build-only"]`，`docker compose up` 时不会启动。**首次部署必须先执行 `docker compose --profile build-only build` 构建基础镜像**，再执行 `docker compose build` 构建服务镜像；直接执行 `docker compose build` 不会触发基础镜像构建，服务镜像会因找不到 `FROM` 的基础镜像而失败。服务镜像的 `FROM` 指令直接使用 `image` 字段声明的镜像名引用本地已构建的基础镜像。
> - `crawler-service` 使用 `network_mode: host` 以访问宿主机 Chrome 的 CDP 端口（9222），因此不使用 `ports` 映射。
> - `processed_jobs.json` 需提前在宿主机创建合法的空 JSON 文件（`echo '{}' > ./data/processor-service/processed_jobs.json`），否则 Docker 会将其创建为目录；使用 `touch` 创建的空文件不是合法 JSON，服务启动时会解析失败。服务代码在加载该文件时应做防御性处理：若文件为空或解析失败，则初始化为 `{}`。建议使用项目根目录提供的 `init.sh` 脚本一次性完成所有必要文件和目录的初始化：
>   ```bash
>   #!/usr/bin/env bash
>   # init.sh —— 首次部署前执行，创建所有必要的宿主机目录和文件
>   set -e
>   mkdir -p data/crawler
>   mkdir -p data/data-verify
>   mkdir -p data/post-process/data
>   mkdir -p data/post-process/output
>   mkdir -p data/processor-service/cache
>   mkdir -p logs/crawler-service
>   mkdir -p logs/processor-service
>   # 日志目录设为全局可写，确保容器内进程（无论以何种用户运行）均可写入宿主机挂载的日志目录
>   chmod 777 logs/crawler-service logs/processor-service
>   touch data/data-verify/loss.txt           # 空文件合法，校验时视为无缺失（PASS）
>   touch data/data-verify/validation_errors.txt  # 空文件合法
>   echo '{}' > data/processor-service/processed_jobs.json  # 必须写入 {}，不可用 touch（空文件非合法 JSON）
>   echo "初始化完成"
>   ```
> **注意**：`loss.txt` 和 `validation_errors.txt` 用 `touch` 创建空文件即可，空文件是合法的（校验时逐行解析，空文件等同于无缺失）；`processed_jobs.json` 必须写入 `{}`，不可用 `touch`，否则服务启动时 JSON 解析失败。
> - 宿主机目录结构建议：
>   ```
>   auto-crawler/
>   ├── data/
>   │   ├── crawler/                          # 爬虫原始数据
>   │   ├── data-verify/
>   │   │   ├── loss.txt                      # 需提前 touch 创建
>   │   │   └── validation_errors.txt         # 需提前 touch 创建
>   │   ├── post-process/
>   │   │   ├── data/
>   │   │   └── output/
>   │   └── processor-service/
>   │       ├── cache/
>   │       └── processed_jobs.json           # 需提前写入 {} 创建（不可用 touch，空文件非合法 JSON）
>   ├── logs/
>   │   ├── crawler-service/
>   │   └── processor-service/
>   └── .env
>   ```

---

### 七、关键实现细节

**1. 两种触发模式的入口逻辑**

```python
# main.py（服务器 A）
import argparse, sys, yaml
import time
from datetime import date, timedelta
from apscheduler.schedulers.blocking import BlockingScheduler
from apscheduler.triggers.cron import CronTrigger

def parse_args():
    parser = argparse.ArgumentParser()
    parser.add_argument("--mode", choices=["scheduled", "batch"], default="scheduled")
    parser.add_argument("--start", help="批量模式起始日期 YYYY-MM-DD")
    parser.add_argument("--end",   help="批量模式结束日期 YYYY-MM-DD")
    parser.add_argument("--task",  help="批量模式任务名，逗号分隔；不指定则爬取全部")
    return parser.parse_args()

if __name__ == "__main__":
    from config_loader import load_config
    from logger import setup_logger
    import pipeline
    config = load_config("config.yaml")
    logger = setup_logger("main", config["log_dir"])
    args = parse_args()

    logger.info(f"crawler-service 启动，mode={args.mode}")

    if args.mode == "scheduled":
        # 调度配置完全由 crawler-service/config.yaml 的 schedule 块控制
        sched_cfg = config["schedule"]
        cron_expr    = sched_cfg.get("cron", "30 8 * * *")
        timezone     = sched_cfg.get("timezone", "Asia/Shanghai")
        offset_days  = sched_cfg.get("date_offset_days", -1)

        logger.info(f"定时日增模式已启动，cron={cron_expr}，timezone={timezone}，date_offset_days={offset_days}，等待首次触发...")

        def scheduled_job():
            target = (date.today() + timedelta(days=offset_days)).strftime("%Y-%m-%d")
            logger.info(f"定时任务触发，目标日期={target}")
            # tasks=None 时 pipeline 内部从 crawler/config.yaml 读取所有启用任务名作为 categories
            pipeline.run(start=target, end=target, tasks=None)

        scheduler = BlockingScheduler(timezone=timezone)
        scheduler.add_job(scheduled_job, CronTrigger.from_crontab(cron_expr, timezone=timezone))
        scheduler.start()
    else:
        # 手动批量：按指定日期范围和任务范围爬取，立即执行一次后退出
        if not args.start or not args.end:
            logger.error("batch 模式必须同时指定 --start 和 --end 参数")
            sys.exit(1)
        tasks = args.task.split(",") if args.task else None
        logger.info(f"手动批量模式，start={args.start}，end={args.end}，tasks={tasks or '全部'}")
        pipeline.run(start=args.start, end=args.end, tasks=tasks)
```

**2. 校验结果判断 + 批量重爬（避免多次重复操作）**

```python
# verify_runner.py —— 解析 loss.txt，判断校验是否通过；封装 analyze_excel.py 调用

# loss.txt 格式：
#   # 注释行（以 # 开头，忽略）
#   # 生成时间: 2026-03-13 18:04:11
#   # 格式: 名称,日期,通道名称
#   （空行，忽略）
#   名称,日期[,通道名称]   ← 有效数据行
#
# 判断规则：跳过注释行和空行后，若无任何有效数据行 → PASS，否则 → FAIL

def is_pass(loss_path: str) -> bool:
    """loss.txt 中无有效数据行则返回 True（校验通过）"""
    missing = []
    with open(loss_path, encoding="utf-8") as f:
        for line in f:
            line = line.strip()
            if line and not line.startswith("#"):
                missing.append(line)
    if missing:
        logger.debug(f"loss.txt 有效数据行（共 {len(missing)} 条）：{missing[:10]}{'...' if len(missing) > 10 else ''}")
        return False  # 存在有效数据行 → FAIL
    return True  # 全为注释/空行 → PASS

# 汇总所有失败项，供 --loss-file 参数使用
def parse_loss_file(loss_path) -> dict[str, list[tuple]]:
    """返回 {task_name: [(date, channel_or_None), ...]}"""
    ...
```

```python
# pipeline.py —— 完整的重试循环（含轮次控制、等待、循环校验）
# 流程：初始校验 → [若失败] 第1轮重爬→校验 → [若失败] 第2轮重爬→校验 → [若失败] 第3轮重爬→校验 → 循环外最终判断
# 共执行最多 max_retry_rounds 次重爬，最多 max_retry_rounds+1 次校验
import os, time, logging
import crawler_runner, verify_runner

logger = logging.getLogger(__name__)

def run_verify_and_retry(start: str, end: str, config: dict):
    """执行校验，失败则批量重爬，最多重试 max_retry_rounds 轮"""
    max_rounds = config["verify"]["max_retry_rounds"]        # 默认 3
    interval   = config["verify"]["retry_interval_seconds"]  # 默认 30
    loss_path  = config["verify"]["loss_file"]

    logger.info(f"[Step 2] 开始数据校验，日期范围={start}~{end}")
    verify_runner.run(start=start, end=end)   # 第一次校验
    if verify_runner.is_pass(loss_path):
        logger.info("[Step 2] 初始校验通过，进入打包上传步骤")
        return True

    # 统计缺失条数用于 WARNING 日志
    with open(loss_path, encoding="utf-8") as f:
        missing_lines = [l.strip() for l in f if l.strip() and not l.startswith("#")]
    logger.warning(f"[Step 2] 初始校验失败，缺失 {len(missing_lines)} 条，进入重爬流程")

    still_missing = missing_lines  # 初始化，防止 max_rounds=0 时循环不执行导致 NameError
    for round_num in range(1, max_rounds + 1):
        logger.warning(f"[Step 3] 第 {round_num}/{max_rounds} 轮重爬开始，loss_file={loss_path}")
        # 将 loss.txt 绝对路径传给爬虫，批量补充下载
        crawler_runner.run_with_loss_file(os.path.abspath(loss_path))
        logger.info(f"[Step 3] 第 {round_num} 轮重爬完成，开始校验")
        verify_runner.run(start=start, end=end)
        if verify_runner.is_pass(loss_path):
            logger.info(f"[Step 3] 第 {round_num} 轮重爬后校验通过，进入打包上传步骤")
            return True
        with open(loss_path, encoding="utf-8") as f:
            still_missing = [l.strip() for l in f if l.strip() and not l.startswith("#")]
        logger.warning(f"[Step 3] 第 {round_num} 轮校验仍不通过，剩余缺失 {len(still_missing)} 条")
        if round_num < max_rounds:
            logger.info(f"[Step 3] 等待 {interval}s 后开启第 {round_num + 1} 轮重爬...")
            time.sleep(interval)

    # 超出最大轮次且仍不通过
    logger.error(f"[Step 3] 已达最大重试轮次 {max_rounds}，仍存在缺失数据（{len(still_missing)} 条），继续执行打包上传。缺失列表：{still_missing}")
    return False
```

**3. MinIO 打包上传（uploader.py 示例）**

```python
# uploader.py（两个服务通用）
import os, tarfile, hashlib, tempfile, logging
from minio import Minio

logger = logging.getLogger(__name__)

def get_client(config: dict) -> Minio:
    return Minio(
        endpoint=config["endpoint"].replace("http://", "").replace("https://", ""),
        access_key=config["access_key"],
        secret_key=config["secret_key"],
        secure=config.get("secure", False),
    )

def _md5_file(path: str) -> str:
    """分块计算文件 MD5，避免大文件一次性读入内存导致 OOM。"""
    h = hashlib.md5()
    with open(path, "rb") as f:
        for chunk in iter(lambda: f.read(8192), b""):
            h.update(chunk)
    return h.hexdigest()

def pack_and_upload(source_dir: str, object_name: str, config: dict) -> tuple[str, int]:
    """将 source_dir 目录整体打包为 .tar.gz 后上传到 MinIO，保留目录结构。
    先写临时文件再上传，避免大数据量时全量加载到内存导致 OOM。
    返回 (md5, size_bytes)。
    """
    logger.info(f"[Step 4] 开始打包，source_dir={source_dir}，object_name={object_name}")
    with tempfile.NamedTemporaryFile(suffix=".tar.gz", delete=False) as tmp:
        tmp_path = tmp.name
    try:
        with tarfile.open(tmp_path, "w:gz") as tar:
            # arcname 使用目录名，解压后保留子目录结构
            tar.add(source_dir, arcname=os.path.basename(source_dir))

        size_bytes = os.path.getsize(tmp_path)
        md5 = _md5_file(tmp_path)
        logger.debug(f"打包完成，tmp_path={tmp_path}，size={size_bytes} bytes，md5={md5}")

        full_object = f"{config['prefix']}{object_name}"
        logger.info(f"[Step 4] 开始上传，bucket={config['bucket']}，object={full_object}，size={size_bytes} bytes")
        client = get_client(config)
        client.fput_object(
            bucket_name=config["bucket"],
            object_name=full_object,
            file_path=tmp_path,
            content_type="application/gzip",
        )
        logger.info(f"[Step 4] 上传完成，object={full_object}，md5={md5}，size={size_bytes} bytes")
        return md5, size_bytes
    except Exception as e:
        logger.error(f"[Step 4] 上传失败，object_name={object_name}，错误：{e}", exc_info=True)
        raise
    finally:
        os.unlink(tmp_path)
        logger.debug(f"临时文件已清理：{tmp_path}")
```

**注意**：MinIO 的 `access_key` / `secret_key` 等敏感配置应通过环境变量注入，配置文件中使用占位符，例如：
```yaml
upload:
  access_key: "${CRAWLER_MINIO_ACCESS_KEY}"
  secret_key: "${CRAWLER_MINIO_SECRET_KEY}"
```

**4. 配置文件加载与环境变量替换**

Python 的 `yaml.safe_load()` 不会自动展开 `${VAR}` 占位符，需在加载后手动替换。两个服务均使用以下工具函数加载配置，**不得直接使用 `yaml.safe_load()` 读取配置文件**：

```python
# config_loader.py（两个服务通用）
import os, re, yaml, logging

_logger = logging.getLogger(__name__)

def load_config(path: str) -> dict:
    """加载 YAML 配置，自动将 ${ENV_VAR} 占位符替换为对应环境变量值。
    若环境变量未设置，保留原始占位符字符串并记录 WARNING 日志，便于排查配置问题。
    """
    with open(path, encoding="utf-8") as f:
        raw = f.read()

    def _replace(m):
        var_name = m.group(1)
        val = os.environ.get(var_name)
        if val is None:
            _logger.warning(f"环境变量 {var_name} 未设置，配置中保留原始占位符")
            return m.group(0)
        return val

    raw = re.sub(r'\$\{(\w+)\}', _replace, raw)
    return yaml.safe_load(raw)
```

`main.py` 中将 `yaml.safe_load(open("config.yaml"))` 替换为：

```python
from config_loader import load_config
config = load_config("config.yaml")
```

**5. 服务间通知（HTTP 回调示例）**

```python
# notifier.py（服务器 A）
import httpx, time, logging
from datetime import datetime

logger = logging.getLogger(__name__)

def notify_processor(object_name: str, md5: str, start: str, end: str,
                     categories: list[str], config: dict):
    prefix = config['upload']['prefix'].lstrip('/')
    payload = {
        "object_name": object_name,
        "download_url": f"{config['upload']['endpoint']}/{config['upload']['bucket']}/{prefix}{object_name}",
        "md5": md5,
        "date_range": {"start": start, "end": end},
        "categories": categories,
        "timestamp": datetime.now().isoformat()
    }
    headers = {"X-Secret": config["notify"]["secret"]}
    retry_times = config["notify"].get("retry_times", 3)
    retry_interval = config["notify"].get("retry_interval_seconds", 30)
    url = config["notify"]["url"]

    logger.info(f"[Step 4] 通知服务器 B，url={url}，object_name={object_name}，categories={categories}")
    logger.debug(f"通知 payload：{payload}")

    for attempt in range(retry_times):
        try:
            with httpx.Client(timeout=30) as client:
                resp = client.post(url, json=payload, headers=headers)
                logger.debug(f"通知响应，status={resp.status_code}，body={resp.text[:200]}")
                if resp.status_code == 409:
                    logger.info("服务器 B 已在处理该任务（幂等），视为通知成功")
                    return
                resp.raise_for_status()
            logger.info(f"[Step 4] 通知服务器 B 成功，object_name={object_name}")
            return
        except Exception as e:
            if attempt < retry_times - 1:
                logger.warning(f"通知服务器 B 失败（第 {attempt + 1}/{retry_times} 次），{retry_interval}s 后重试，错误：{e}")
                time.sleep(retry_interval)
            else:
                logger.error(f"通知服务器 B 失败，已达最大重试次数 {retry_times}，url={url}，错误：{e}", exc_info=True)
```

**6. 三方平台通知重试（固定间隔）**

```python
# platform_notifier.py（服务器 B）
import time, httpx, logging

logger = logging.getLogger(__name__)

def notify_with_retry(platform: dict, payload: dict):
    retry_times = platform.get("retry_times", 5)
    retry_interval = platform.get("retry_interval_seconds", 30)
    name = platform["name"]
    url = platform["url"]

    logger.info(f"[Step 4] 通知三方平台 {name}，url={url}")
    logger.debug(f"通知 payload：{payload}")

    for attempt in range(retry_times):
        try:
            resp = httpx.post(url, json=payload,
                              headers={"Authorization": f"Bearer {platform['token']}"},
                              timeout=30)
            logger.debug(f"平台 {name} 响应，status={resp.status_code}，body={resp.text[:200]}")
            resp.raise_for_status()
            logger.info(f"[Step 4] 通知平台 {name} 成功")
            return
        except Exception as e:
            if attempt < retry_times - 1:
                logger.warning(f"通知平台 {name} 失败（第 {attempt + 1}/{retry_times} 次），{retry_interval}s 后重试，错误：{e}")
                time.sleep(retry_interval)
            else:
                logger.error(f"通知平台 {name} 失败，已达最大重试次数 {retry_times}，url={url}，错误：{e}", exc_info=True)
```

---

### 八、输出要求

请生成以下文件：

1. `crawler-service/base/Dockerfile` — 采集服务基础镜像（Chrome/Playwright + 公共依赖）
2. `crawler-service/base/requirements-base.txt` — 采集服务基础镜像 Python 依赖
3. `crawler-service/main.py` — 定时调度主入口
4. `crawler-service/pipeline.py` — 完整流程编排（爬取→校验→重爬→打包上传→通知）
5. `crawler-service/crawler_runner.py` — 爬虫调用封装
6. `crawler-service/verify_runner.py` — 校验调用封装（含失败项解析）
7. `crawler-service/uploader.py` — 打包上传（支持 S3/SFTP/本地三种模式）
8. `crawler-service/notifier.py` — 通知服务器 B
9. `crawler-service/config_loader.py` — 配置加载工具（含环境变量替换）
10. `crawler-service/logger.py` — 日志初始化工具（`setup_logger`，同时输出到控制台和滚动文件，支持 `LOG_LEVEL` 环境变量覆盖）
11. `crawler-service/config.yaml` — 服务配置模板
12. `crawler-service/requirements.txt` — 业务层追加依赖
13. `crawler-service/Dockerfile` — 服务镜像
14. `processor-service/base/Dockerfile` — 处理服务基础镜像（公共依赖，无 Chrome）
15. `processor-service/base/requirements-base.txt` — 处理服务基础镜像 Python 依赖
16. `processor-service/main.py` — HTTP 服务主入口
17. `processor-service/api.py` — 触发接口（FastAPI）
18. `processor-service/processor_runner.py` — 数据转换调用封装
19. `processor-service/uploader.py` — 打包上传（支持 S3/SFTP/本地三种模式）
20. `processor-service/platform_notifier.py` — 三方平台通知（含重试）
21. `processor-service/config_loader.py` — 配置加载工具（含环境变量替换）
22. `processor-service/logger.py` — 日志初始化工具（同上，两个服务各自独立一份）
23. `processor-service/config.yaml` — 服务配置模板
24. `processor-service/requirements.txt` — 业务层追加依赖
25. `processor-service/Dockerfile` — 服务镜像
26. `docker-compose.yml` — 统一部署入口（两个服务的构建与启动）
27. `.env.example` — 敏感配置模板（占位符，可提交版本库；实际使用时复制为 `.env` 并填入真实值）
28. `init.sh` — 首次部署初始化脚本（创建宿主机所需目录和文件，包括 `processed_jobs.json` 的合法空 JSON 初始化）

---

### 九、约束与注意事项

- 不修改 `crawler/`、`data-verify/`、`post-process/` 目录下的任何已有文件
- 两个微服务通过配置文件中的路径引用已有脚本，使用 `subprocess.run()` 调用
- 校验失败重爬必须批量处理，不得对每个失败文件单独触发一次完整流程
- 所有敏感配置（token、secret、bucket 等）通过配置文件或环境变量注入，不硬编码
- 日志统一格式：`%(asctime)s [%(levelname)s] %(name)s: %(message)s`
- 日志初始化统一通过 `logger.py` 中的 `setup_logger(name, log_dir, level=None)` 完成，不得在各模块内直接调用 `logging.basicConfig()`；`level` 默认读取环境变量 `LOG_LEVEL`（不区分大小写），未设置时默认 `INFO`
- 每个步骤（Step 1～4）的开始、完成、跳过均需以 `[Step N]` 前缀写入 `INFO` 日志，便于在日志中快速定位流程阶段
- subprocess 调用（爬虫、校验、转换脚本）必须捕获并记录 stdout/stderr：正常退出时以 `DEBUG` 级别输出完整 stdout；非零退出时以 `ERROR` 级别输出 returncode 和完整 stderr，并抛出异常终止当前步骤
- 所有异常捕获处（`except Exception`）必须使用 `logger.error(..., exc_info=True)` 输出完整堆栈，不得仅记录 `str(e)`
- 重试类操作（通知重试、重爬轮次）必须在每次重试时以 `WARNING` 记录当前次数、最大次数和失败原因，最终成功时以 `INFO` 确认
- 每个步骤的执行结果（成功/失败/跳过）均需写入日志，便于排查问题
- 两个微服务的通知功能均支持开关控制：服务器 A 通过 `notify.enabled` 控制是否通知服务器 B；服务器 B 通过每个平台条目的 `enabled` 字段单独控制各三方平台的通知，关闭时跳过对应平台，不影响其他平台和后续流程
- 两个微服务均支持上传后清理本地数据：服务器 A 通过 `crawler.cleanup_after_upload` 控制是否在打包上传完成后清除 `crawler/data/` 目录；服务器 B 通过 `processor.cleanup_after_upload` 控制是否清除 `post-process/data/` 和 `post-process/output/` 目录；清理操作仅在上传成功后执行，上传失败时不清理
